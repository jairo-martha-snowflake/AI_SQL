use role accountadmin;

CREATE OR REPLACE API INTEGRATION jmt_git_api_integration
  API_PROVIDER = git_https_api
API_ALLOWED_PREFIXES = ('https://github.com/jairo-martha-snowflake')
  ALLOWED_AUTHENTICATION_SECRETS = (main_login)
  ENABLED = TRUE;


CREATE OR REPLACE SECRET my_git_secret
  TYPE = password
  USERNAME = 'gladyskravitz'
  PASSWORD = 'ghp_token';